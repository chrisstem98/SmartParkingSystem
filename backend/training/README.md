# Training Notes

This folder contains documentation and scripts for training YOLO models on the PKLot dataset.

- `colab_yolov8_pklot.md`: step-by-step Colab instructions.
- Model weights (`*.pt`) are NOT committed. Place trained weights under `backend/models/` locally.
