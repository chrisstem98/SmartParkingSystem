# Colab: YOLOv8 Training on PKLot

## Environment
- Runtime: GPU
- `pip install ultralytics --upgrade`

## Dataset
- Zip your local `data/datasets/PKLot/` and upload to Google Drive as `PKLot.zip`
- Unzip in Colab to `/content/datasets/PKLot/`
- Ensure `data.yaml` points to `train/`, `valid/`, `test/` images

## Train (example)
